{
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "test": "node --import tsx --import ./tests/browser-globals.ts --test tests/**/*.test.ts",
    "test:typecheck": "tsc -p tsconfig.tests.json",
    "bot:diagnostics": "node --import tsx ./scripts/run-bot-diagnostics.ts"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@types/jsdom": "^28.0.3",
    "@types/node": "^26.1.1",
    "jsdom": "^29.1.1",
    "phaser3spectorjs": "^0.0.8",
    "tsx": "^4.22.4"
  }
}
